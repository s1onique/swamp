# MATRIX — Architecture × Signal × ANSI results## Architecture × Signal × Test result matrix```              | x86_64 (Rosetta) | arm64 (native)--------------|------------------|----------------M1 child.kill | EPERM, 5006ms    | EPERM, 5006msM2 Deno.kill  | EPERM, 5003ms    | EPERM, 5001msM3 child.kill | EPERM, 5002ms    | EPERM, 5003msM4 Deno.kill  | EPERM, 5002ms    | EPERM, 5003ms```All 8 cells identical: EPERM on signal delivery, child ran full 5s timeout.## Architecture × Test outcome matrix```                          | x86_64 (Rosetta) | arm64 (native)--------------------------|------------------|----------------doctor isolated N=5       | 5/5 FAILED       | 5/5 FAILEDansi   isolated N=5       | 5/5 FAILED       | 5/5 FAILEDtelemetry isolated        | 2/2 PASS         | (not run, B-cell suffices)```Defects reproduce on both architectures. Substrate defect, notruntime-architecture-dependent.## Harness exit-code audit| iteration | x86_64 exit (legacy harness) | x86_64 exit (CORRECTED) | arm64 exit (CORRECTED) ||-----------|------------------------------|--------------------------|------------------------|| 1         | (not authoritative)          | 1                        | 1                      || 2         | (not authoritative)          | 1                        | 1                      || 3         | (not authoritative)          | 1                        | 1                      || 4         | (not authoritative)          | 1                        | 1                      || 5         | (not authoritative)          | 1                        | 1                      |CORRECTION01 harness: `set +e` / `RC=$?` / `set -e` immediately after thedeno test command. Authority: `(FAILED summary, exit 1)` agrees with`(FAILED summary, exit != 0)` invariant.## Speed comparison (arm64 vs x86_64)| test            | x86_64 mean | arm64 mean | speedup ||-----------------|-------------|------------|---------|| doctor isolated | 61443 ms    | 61105 ms   | ~0% (within noise) || ansi   isolated |  7130 ms    |   378 ms   | ~19×   |ANSI on arm64 is dramatically faster (no Rosetta translation overhead).Doctor timing is dominated by the 30s setTimeout in the child, masking anyRosetta drag.## Conservation```doctor isolated: 11 + 2 == 13   (PASS)ansi   isolated: 47 + 2 == 49   (PASS)classified targets: 4          (now 2 environmental + 2 deno-behavior)```## Decision tree outcome```both runtimes reproduce doctor failure+ microreproducer shows EPERM (substrate)+ shell kill -TERM also returns EPERM (substrate)    ↓DOCTOR_REPRODUCIBLE_DEFECT → reclassified ENVIRONMENTAL_SANDBOX_BLOCKS_SIGNAL                              (not a Swamp defect; substrate must be fixed                               for the test to pass on this host)both runtimes reproduce ANSI failure+ NO_COLOR=1 / TERM=dumb do not change outcome+ od -c shows literal ESC bytes regardless of env    ↓ANSI_REPRODUCIBLE_DEFECT  → CONFIRMED as deno-behavior issue                              (Swamp test/production fix required;                               not substrate, not Rosetta)```