# Factory Epic Board — Swamp

| ID               | State   | Purpose                                                  |
| ---------------- | ------- | -------------------------------------------------------- |
| SWAMP-BASELINE01                | CLOSED  | Establish pristine executable baseline and authority map; corrected by CORRECTION01 and CORRECTION02 |
| SWAMP-BASELINE01-CORRECTION01    | CLOSED  | Reconcile arithmetic, fix board state, fix patch hygiene — superseded by CORRECTION02 for telemetry classification |
| SWAMP-BASELINE01-CORRECTION02    | CLOSED  | Re-classify telemetry failures by observed signature; codify evidence-immutability doctrine |
| SWAMP-BASELINE01-CORRECTION03    | CLOSED  | Scope the evidence-hygiene verifier to the policy it enforces (policy == verifier scope == reported claim) |
| SWAMP-TEST-CHAR01                | CLOSED  | Characterize test-suite failures via 4-cell cache × HOME matrix. Verdict: TEST_SUITE_HAS_REPRODUCIBLE_DEFECTS. 2 doctor + 2 ANSI defects all reproducible. 154 mkdir family confirmed environmental. Substrate-bound — see CORRECTION01. |
| SWAMP-TEST-CHAR01-CORRECTION01    | CLOSED  | Fix harness exit-code capture (DONE), re-run doctor+ANSI on native arm64 (DONE), Swamp-free subprocess-signal microreproducer on x86_64+arm64 (DONE). Verdict: TEST_CHARACTERIZATION_CORRECTED_TO_SUBSTRATE_AND_DENO_BEHAVIOR. D1/D2 doctor reclassified to ENVIRONMENTAL_SANDBOX_BLOCKS_SIGNAL. A1/A2 ANSI reclassified to DENO_BEHAVIOR_NO_COLOR_NOT_HONORED. |
| SWAMP-DOCTOR-SIGNAL-CAPABILITY01  | CLOSED  | Make `runChildWithAbort` capability-aware and fail-explicit. Verdict: SIGNAL_CAPABILITY_HANDLING_REPAIRED. Added `ChildSignalDeliveryError` + `SignalAttempt` discriminated outcome + `_signalSender` test seam + capability probe. Portable tests T1-T8 deterministic; real-signal tests capability-gated. Denied-signal latency: 5285 ms → 32-114 ms. Substrate defect preserved (CAUSE_OWNER=SUBSTRATE, HANDLING_QUALITY=ROBUST). |
| SWAMP-DOCTOR-SIGNAL-CAPABILITY01-CORRECTION01 | CLOSED | Repair 3 Factory closure defects. Verdict: FACTORY_CLOSURE_PACKET_REPAIRED. (1) Commit 13 raw evidence files (was untracked). (2) Regenerate `raw-sha256.txt` excluding itself (was self-referential). (3) Split runner status (24 passed / 0 failed / 0 ignored) from semantic coverage (22 portable executed + 0 real-signal executed + 2 capability-unavailable). No production code changed. Verifier: `.factory/scripts/check_signal_capability01_factory_closure.sh` — 9/9 invariants PASS. |
| SWAMP-ANSI-OUTPUT01                 | CLOSED  | Verdict: ANSI_OUTPUT_NORMALIZATION_REPAIRED. Cause owner DEPENDENCY_BEHAVIOR (Deno emits ANSI despite NO_COLOR=1); handling quality went from UNNORMALIZED → ROBUST. Added `normalizeExternalDiagnostic` (uses `@std/fmt/colors` `stripAnsiCode`, already in import map) at the two producer sites (fmt failure, lint failure); NO_COLOR=1 preserved as best-effort request. Test file went 47 passed + 2 failed → 58 passed + 0 failed; broader extension group 129 passed + 0 failed. deno check / lint / fmt --check all exit 0. Production diff = 2 files only (`extension_quality_checker.ts` + `extension_quality_checker_test.ts`); no deno.json / deno.lock / AGENTS.md / verification changes. Raw evidence 37 files hashed + verified. |
| SWAMP-CHARACTERIZE-REST01 | CLOSED | Verdict: REMAINING_FAILURE_SURFACE_MIXED. Subject `a392c49e`. Natural completion of the full native suite under prewarmed external cache (`/tmp/swamp-char-rest01.ipyvth/deno`) + synthetic writable HOME + native arm64 Deno 2.9.7 + `--parallel`. Runner summary: `FAILED | 12287 passed (214 steps) | 33 failed | 30 ignored (1 step) (10m11s)`. Conservation holds. 33 failures → 3 clusters: CLUSTER-01 (31 ENVIRONMENTAL/FALSIFIED_BY_CONTROL), CLUSTER-02 (1 UNRESOLVED/UNKNOWN — corrected by CORRECTION01), CLUSTER-03 (1 TEST_CONTRACT_AMBIGUITY — corrected by CORRECTION01). New verifier `.factory/scripts/check_characterize_rest01.sh` — 23/23 PASS. **SUPERSEDED in part by SWAMP-CHARACTERIZE-REST01-CORRECTION01 (cluster-02 evidence overstated; cluster-03 re-framed; dogfood_ready_reason re-derived).** |
| SWAMP-CHARACTERIZE-REST01-CORRECTION01 | CLOSED | Verdict: REMAINING_FAILURE_SURFACE_MIXED (parent verdict unchanged; classification inventory corrected). Subject `a392c49e`. Corrects three review-discovered inconsistencies in parent ACT without production changes. CLUSTER-02: `evidence` `REPRODUCED_REPEATEDLY` → `OBSERVED_ONCE_UNDER_FULL_SUITE_LOAD`; `cause_owner` `SUBSTRATE` → `UNKNOWN`; primary `ENVIRONMENTAL` → `UNRESOLVED`. CLUSTER-03: primary `PROJECT_DEFECT` → `TEST_CONTRACT_AMBIGUITY`; framing renamed from "timing edge" to "fixture asserts scenario A while test name speaks scenario B"; recommended ACT `SWAMP-DOCTOR-PORTABLE-TIMING01` → `SWAMP-DOCTOR-TERMINAL-RACE01`. DOGFOOD_READY: previously `false` despite every gate satisfied; now `false` causally earned (unresolved > 0). 10 control runs executed: 5/0 isolated single (DENO_JOBS=1/2/default ×3 --parallel), 55/1 then 56/0 ×2 neighborhood co-run, 328/0 worker-class co-run, 363/0 full integration/ co-run. No controlled subset reproduces cluster-02's RPC channel close. Conservation holds (33 = 31 ENV + 1 UNRESOLVED + 1 TEST_CONTRACT_AMBIGUITY). New verifier `.factory/scripts/check_characterize_rest01_correction01.sh`. Recommend next ACTs (one of two, parallel): `SWAMP-REMOTE-PARALLEL-INTERFERENCE01` (cluster-02 bisect) or `SWAMP-DOCTOR-TERMINAL-RACE01` (cluster-03 contract). **SUPERSEDED in machine projection by SWAMP-CHARACTERIZE-REST01-CORRECTION02 (four projection defects remained in the parent correction ACT; see that row).** |
| SWAMP-CHARACTERIZE-REST01-CORRECTION02 | CLOSED | Verdict: REMAINING_FAILURE_SURFACE_MIXED (parent verdict unchanged; machine projection authority restored). Subject `a392c49e`. Factory-only; no new reproduction experiment. Repairs FOUR projection defects left by CORRECTION01: D1 `failures.json` F-33 `evidence_strength` still `REPRODUCED_REPEATEDLY` (now `OBSERVED_ONCE_UNDER_FULL_SUITE_LOAD`); D2 top-level `classification_conservation` still `{PROJECT_DEFECT:1,ENVIRONMENTAL:32}` (now `{ENVIRONMENTAL:31,UNRESOLVED:1,TEST_CONTRACT_AMBIGUITY:1}`); D3 `classified-inventory.json` per-cluster rows still labeled with old classifications (rebuilt from failures.json as single source of truth); D4 ambiguous `unknown_red` field removed entirely from both files (kept only formally-defined `no_unknown_red`); D5 verifier rewritten as predicate→assertion→exit (was: unconditional `add ok`). Also narrows CLUSTER-03.md wording: "production code is correct" → "observed failure is not evidence of production-code failure" (a weaker, defensible claim). New verifier `.factory/scripts/check_characterize_rest01_correction02.sh` — 28/28 PASS, and demonstrably CAN FAIL (intentional divergence test passed). Recommend next ACT: `SWAMP-REMOTE-PARALLEL-INTERFERENCE01` (CLUSTER-02 is the only unknown-red blocker; CLUSTER-03 is bounded and not blocking). **SUPERSEDED in machine projection by SWAMP-CHARACTERIZE-REST01-CORRECTION03 (six closure-authority defects remained: see that row).** |
| SWAMP-CHARACTERIZE-REST01-CORRECTION03 | CLOSED_PENDING_ATTESTATION | Verdict: CLOSURE_STATE_AUTHORITY_RESTORED (pending attestation commit). Subject `a392c49e`. Factory-only; no new reproduction experiment. Repairs SIX closure-authority defects left by CORRECTION02: D1 parent-hash preservation used existence-only fallback; replaced with independent historical blob comparison against `git show <parent>:<path>` and SHA256 compare; D2 verifier cannot evidence its own post-commit cleanliness from inside the content commit; introduced explicit `--mode precommit|postcommit` separation plus two-commit protocol (content commit + attestation commit); D3 projection cardinality wording inconsistent ("seven projections" but eight enumerated); canonical count derived from `manifest.json` registry = 8; D4 verifier-count projections disagreed (28/28 vs 47/47); introduced single-machine-projection final lines `VERIFIER_TOTAL/PASS/FAIL/RESULT` plus no-stale-text guard; D5 mixed pre/post-commit predicates in one invariant set; explicit separation with `WORKING_TREE_CLEAN_AT_MEASUREMENT` only required in postcommit mode and `WORKING_TREE_CLEAN_AT_FINAL_COMMIT` removed entirely from precommit; D6 CORRECTION02 raw divergence evidence not distinguished from authoritative post-commit; CORRECTION03 introduces `precommit/`, `parent-hash-negative/`, `projection-negative/`, `postcommit/` subtrees with `EVIDENCE_ROLE` markers. Doctrine upgraded to six properties (adds "temporal/state binding"). New verifier `.factory/scripts/check_characterize_rest01_correction03.sh`. Content commit TBD; attestation commit TBD; projection count 8 (derived); verifier counts runtime-derived. Recommend next ACT: `SWAMP-REMOTE-PARALLEL-INTERFERENCE01` (CLUSTER-02 remains sole unknown-red blocker). |

| SWAMP-DOGFOOD01  | QUEUED  | Use Swamp externally as intended                         |
| SWAMP-ATTACK01   | QUEUED  | Adversarially falsify important guarantees               |
| SW-F01           | BACKLOG | Immutable/versioned Factory evidence experiment          |
| SW-F02           | BACKLOG | Deterministic attestation projection                     |
| SW-CM01          | BACKLOG | Harness/skill trigger evaluation                         |
| SW-CM04          | BACKLOG | ClineMM premature-handoff behavioral corpus              |
| SW-X01           | BACKLOG | Compile cognition away experiment                        |
| SW-M01           | BACKLOG | Typed workflow IR                                        |
| SW-M02           | BACKLOG | Gate-dominance proof                                     |

Notes on each ACT are deliberately short — only enough to preserve intent.
The baseline ACT (this one) carries the real plan in
`.factory/acts/SWAMP-BASELINE01.md`.

## Recommended ordering (operator's discretion)

1. `SWAMP-BASELINE01` (this ACT) — done; verdict is `BASELINE_ESTABLISHED_WITH_GAPS`.
2. *Pre-DOGFOOD work*: resolve the test-suite fragility (F1/F3 in
   FINDINGS.md) in a writable-`~/.claude/` environment. This is the
   highest-value pre-work because the upstream PR's claimed 12 288
   passes differs from this baseline's 12 128 passes; that gap must
   be explained before dogfooding on top of it.
3. `SWAMP-DOGFOOD01` — with the test suite trusted.
4. `SWAMP-ATTACK01` — once the baseline + dogfood establish what
   normal looks like.
5. `SW-F01`, `SW-F02` — Factory-specific extractions.
6. `SW-CM01`, `SW-CM04` — ClineMM-specific.
7. `SW-X01`, `SW-M01`, `SW-M02` — deeper evaluation work.

Do not auto-launch any of these after `SWAMP-BASELINE01` closes —
the ACT explicitly forbids it (`§25`).
